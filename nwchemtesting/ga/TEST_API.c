// A test harness  of my API - more or less does what ga-delta.c does
// but uses the API - i.e., initializes GA/MPI and reads
// stuff into a global array here and then passes the handles and 
// required context.


int main(int argc, char ** argv) {
//Initialize MPI/GA
//Read in files into global arrays, obtain file sizes
//Pass off to my API.
//Terminate MPI/GA
exit(0);
}
